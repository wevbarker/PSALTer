%%============%%
%%  Preamble  %%
%%============%%

\usepackage[utf8]{inputenc}
\usepackage[compat=1.1.0]{tikz-feynman}
\usetikzlibrary{positioning,arrows.meta}
\usetikzlibrary{patterns.meta}

\definecolor{Massless}{RGB}{0,170,0}

\begin{document}
\tikzfeynmanset{ 
	blob/.append style={thick,
		draw=black,
		fill=gray!10,
		%pattern color=black,
		%pattern={Lines[angle=45,line width=0.8pt]},
		minimum size=0.6cm},
	witharrow/.style={
	   decoration={
	     markings,
	     mark=at position 0.5
		  with {\arrow[xshift=2mm]{Stealth[width=3mm,length=4mm]}}
	     },
	   postaction=decorate},
}
\tikzset{
	Tensor/.style={
		decorate,
		decoration={
			snake,
			amplitude=.8mm,
			segment length=1.5mm,
			pre length=.5mm,
			post length=.5mm},
		double	
	},
	Spin3/.style={
		decorate,
		decoration={
			zigzag,
			amplitude=.8mm,
			segment length=1.5mm,
			pre length=.5mm,
			post length=.5mm},
		double
	}
}

\tikzfeynmanset{every vertex/.style={large}}
